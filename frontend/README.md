# LostCats Frontend

## Setup

1. Copy environment variables:

```bash
cp .env.example .env.local
```

2. Install dependencies:

```bash
npm install
```

3. Start the web app:

```bash
npm run dev
```

The UI runs at `http://localhost:3000` and uses `NEXT_PUBLIC_API_BASE_URL`.
